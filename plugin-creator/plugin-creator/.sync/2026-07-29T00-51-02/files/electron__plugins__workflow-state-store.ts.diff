diff --git a/electron/plugins/workflow-state-store.ts b/electron/plugins/workflow-state-store.ts
index b400000..29c4768 100644
--- a/electron/plugins/workflow-state-store.ts
+++ b/electron/plugins/workflow-state-store.ts
@@ -1,6 +1,7 @@
 import fs from 'fs';
 import path from 'path';
-import { StepExecutionResult, WorkflowExecutionResult } from './workflow-types';
+import { BERRYTRACE_HOME } from '../config/paths';
+import type { StepExecutionResult, WorkflowExecutionResult } from './workflow-types';
 
 export interface PersistentRunState {
   runId: string;
@@ -21,7 +22,7 @@ export class WorkflowStateStore {
   private storageDir: string;
 
   private constructor() {
-    this.storageDir = path.resolve(process.cwd(), '.berrytrace', 'workflow_executions');
+    this.storageDir = path.join(BERRYTRACE_HOME, 'workflow_executions');
     if (!fs.existsSync(this.storageDir)) {
       fs.mkdirSync(this.storageDir, { recursive: true });
     }