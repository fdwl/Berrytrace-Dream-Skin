diff --git a/electron/services/updater.ts b/electron/services/updater.ts
index 6d22471..d1349e5 100644
--- a/electron/services/updater.ts
+++ b/electron/services/updater.ts
@@ -1,4 +1,11 @@
-import { app, ipcMain, BrowserWindow } from 'electron'
+import { app, ipcMain, BrowserWindow } from 'electron';
+
+// 独立注册基础 IPC handlers (版本获取与基础 IPC)
+export function registerUpdaterIpc(): void {
+  ipcMain.handle('updater:get-version', () => {
+    return app.getVersion();
+  });
+}
 
 export async function setupAutoUpdater(mainWindow: BrowserWindow): Promise<void> {
   const pkg = await import('electron-updater')
@@ -49,10 +56,6 @@ export async function setupAutoUpdater(mainWindow: BrowserWindow): Promise<void>
     }
   });
 
-  // 注册获取版本号 IPC
-  ipcMain.handle('updater:get-version', () => {
-    return app.getVersion();
-  });
 
   if (app.isPackaged) {
     setTimeout(() => {