diff --git a/electron/plugins/workflow-credential-store.ts b/electron/plugins/workflow-credential-store.ts
index 9dee93e..f40d5f8 100644
--- a/electron/plugins/workflow-credential-store.ts
+++ b/electron/plugins/workflow-credential-store.ts
@@ -1,5 +1,6 @@
 import fs from 'fs';
 import path from 'path';
+import { safeStorage } from 'electron';
 
 export class WorkflowCredentialStore {
   private static instance: WorkflowCredentialStore | null = null;
@@ -18,13 +19,14 @@ export class WorkflowCredentialStore {
     return WorkflowCredentialStore.instance;
   }
 
-  private getSafeStorage(): typeof import('electron').safeStorage | null {
+  private getSafeStorage(): typeof safeStorage | null {
     try {
-      const { safeStorage } = require('electron');
       if (safeStorage && safeStorage.isEncryptionAvailable()) {
         return safeStorage;
       }
-    } catch {}
+    } catch {
+      // safeStorage is not available
+    }
     return null;
   }